# podcast-toolkit 開發約定

> 依據 2026-08-08 全史 354-commit 回顧（返工根因排序：兩套機制互打 > 假完成 > 修了又壞）。

## 開工前
- 先問「這個資料／行為現在有幾個地方在管？」答案 ≥2 → 先併軌再加功能，禁止在互打的機制上疊第三套。
  前科：斷句兩引擎互打（約 23 commits）、編輯器時間 state 雙軌（約 12 commits）。

## 完成的定義（宣告前逐項核對）
- 設定／欄位類改動必驗 round-trip：寫入→重載→讀回原值。episode.yaml 新增可寫鍵時，
  `tests/test_config_roundtrip.py` 會自動列舉寫入端並擋下漏透傳——紅了照失敗訊息補。
- 失敗路徑不准靜默：把失敗 fallback 成表面成功的分支，一律改成有可見錯誤表現。
- UI／版面改動的證據是瀏覽器實測數字＋突變測試（把新規則停掉要能量到退回舊值），不是 diff。

## UX 修改走梯次制
- 小問題累積成清單 → `docs/plans/` 計畫檔 → 一梯批次修 → 驗收時量到的新問題寫進計畫附錄
  留給下一梯，不逐集零星修。範本：`docs/plans/2026-08-08-subtitle-editing-ux.md`。

## 架構觸發條件
- 下次要動編輯器大功能：先拆 app.js（先抽 api.js → render.js；動刀前先補 Playwright 煙霧測試），
  再做功能。回顧顯示 app.js ×108 次改動是返工放大器。
